# 이 파일을 복사해서 .env 로 이름을 바꾼 뒤 실제 키를 넣으세요.
# .env 는 .gitignore 에 등록되어 있어 GitHub 에 올라가지 않습니다.
GEMINI_API_KEY=여기에_발급받은_키를_붙여넣기

# 선택 사항입니다. 비워 두면 앱이 알아서 쓸 수 있는 모델을 고릅니다.
# 여기에는 반드시 '모델 이름'만 넣으세요. API 키를 넣으면 안 됩니다.
# 올바른 예) GEMINI_MODEL=gemini-3.5-flash-lite
GEMINI_MODEL=
